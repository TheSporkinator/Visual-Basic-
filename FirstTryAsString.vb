Module Module 
	Sub Main()
		
		Dim x As Integer = 7
		Dim y As String = "5"

		'Dim myFirstTry As String = CStr(x) & y
		
		Dim myFirstTry As String = x.ToString() & y
		Console.WriteLine(myFirstName)

		Dim mySecondTry As Integer = x + CInt(y)
		Console.WtiteLine(mySecondTry)

		Console.ReadLine()
	
	End Sub
End Module
